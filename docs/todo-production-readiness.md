# 운영 준비 To-Do

## 목적
다양한 검사 링크를 수십 명의 수검자가 실시하고, 응답 저장·채점·결과 제공까지 안정적으로 운영하기 위해 필요한 작업 목록이다.

- 작성일: 2026-05-21
- 기준 상태: FastAPI + React SPA, RDS PostgreSQL, 단일 워커, 관리자 1명

---

## 우선순위 기준

| 등급 | 기준 |
|------|------|
| 🔴 High | 운영 즉시 문제가 되거나 데이터 손실·보안 위험 |
| 🟠 Medium | 규모 증가 시 문제 또는 관리자 운영 불편 |
| 🟡 Low | UX 개선, 있으면 좋음 |

---

## 1. 보안

| # | 항목 | 등급 | 현재 상태 | 할 일 |
|---|------|------|----------|-------|
| S-1 | 관리자 비밀번호 해싱 | 🔴 | SHA256 salt 없음 (TD-010) | `bcrypt` 또는 `argon2-cffi` 도입 |
| S-2 | 관리자 세션 저장소 | 🟠 | 메모리 딕셔너리 (TD-009) | DB 테이블(`admin_session`) 또는 Redis로 이전 |
| S-3 | 검사 제출 rate limiting | 🟠 | 없음 | 동일 IP/토큰에서 단시간 대량 요청 차단 |
| S-4 | 결과 토큰 노출 범위 | 🟠 | 제출 직후 1회 표시, 이후 URL 분실 시 재접근 불가 | 수검자 결과 재접근 방법 정의 (이하 R-3 참고) |

---

## 2. 검사 링크 관리

| # | 항목 | 등급 | 현재 상태 | 할 일 |
|---|------|------|----------|-------|
| L-1 | 링크 만료일 | 🟠 | 없음 (`is_active` 토글만 존재) | `expires_at` 컬럼 추가, 만료된 링크 접속 시 안내 |
| L-2 | 최대 제출 수 제한 | 🟠 | 없음 (무제한) | `max_submissions` 옵션 추가, 초과 시 접속 차단 |
| L-3 | 링크별 실시 현황 | 🟠 | 없음 | 관리자 화면에서 링크별 `완료 n명 / 진행 중 n명` 표시 |
| L-4 | 링크 일괄 비활성화 | 🟡 | 1개씩 토글 | 다중 선택 후 일괄 ON/OFF |

---

## 3. 제출 및 결과 관리

| # | 항목 | 등급 | 현재 상태 | 할 일 |
|---|------|------|----------|-------|
| R-1 | 제출 목록 화면 | 🟠 | 내담자 단위로만 조회 가능 | 링크별 제출 목록 (이름·일시·채점상태) 화면 추가 |
| R-2 | 결과 일괄 내보내기 | 🟠 | 없음 | CSV/Excel 다운로드 (링크별 또는 전체) |
| R-3 | 수검자 결과 재접근 | 🟠 | 제출 직후 URL 1회 표시 | 관리자가 결과 링크를 다시 조회·복사할 수 있는 화면 |
| R-4 | 제출 무효/삭제 처리 | 🟡 | 없음 | 관리자가 특정 제출을 soft delete 처리 |
| R-5 | 재채점 | 🟡 | 없음 | norm 수정 후 기존 제출 일괄 재채점 기능 |

---

## 4. 운영 인프라

| # | 항목 | 등급 | 현재 상태 | 할 일 |
|---|------|------|----------|-------|
| I-1 | 프로세스 관리 | 🔴 | 수동 uvicorn 실행 | `systemd` 서비스 등록 (재부팅 후 자동 재시작) |
| I-2 | HTTPS 적용 | 🔴 | docs에 절차 있으나 미적용 | Let's Encrypt + nginx reverse proxy 설정 완료 |
| I-3 | 에러 모니터링 | 🟠 | 없음 | Sentry 또는 서버 로그 수집 (에러 발생 시 알림) |
| I-4 | DB 자동 백업 | 🟠 | RDS 자동 백업 설정 여부 미확인 | RDS 자동 스냅샷 주기·보관 기간 확인 및 설정 |
| I-5 | 배포 스크립트 | 🟡 | 없음 | 소스 pull → 프런트 빌드 → 서버 재시작을 1개 스크립트로 |

---

## 5. 성능

| # | 항목 | 등급 | 현재 상태 | 할 일 |
|---|------|------|----------|-------|
| P-1 | 채점 동기 처리 | 🟡 | 제출 요청 안에서 채점 완료 후 응답 | 동시 제출 폭증 시: 채점을 백그라운드로 분리 (`asyncio` or Celery) |
| P-2 | 문항 데이터 캐싱 | 🟡 | 매 요청마다 DB 전체 재조회 (TD-006) | 프로세스 메모리 캐시 (검사 20종 이상 또는 응답 지연 체감 시) |
| P-3 | DB 커넥션 풀 튜닝 | 🟡 | SQLAlchemy 기본값 (pool_size=5) | 동시 접속 증가 시 `pool_size`, `max_overflow` 조정 |

---

## 6. 알려진 기술 부채 (docs/technical-debt.md 연동)

| TD | 항목 | 등급 | 상태 |
|----|------|------|------|
| TD-004 | sub_test_json 구조 불일치 (기존/신규) | 🟠 | open |
| TD-006 | 문항·척도 매 요청 재계산, 캐시 없음 | 🟠 | open (재검토 조건 명시됨) |
| TD-009 | 관리자 세션 메모리 저장 | 🟠 | open → S-2 |
| TD-010 | SHA256 salt 없는 비밀번호 해싱 | 🟠 | open → S-1 |

---

## 7. 수검자 UX

| # | 항목 | 등급 | 현재 상태 | 할 일 |
|---|------|------|----------|-------|
| U-1 | 모바일 제출 버튼 | 🔴 | 수정 완료 (2026-05-21) | ✅ done |
| U-2 | 모바일 페이지 이동 버튼 | 🔴 | 수정 완료 (2026-05-21) | ✅ done |
| U-3 | 모바일 집중형 모드 전환 | 🟠 | 수정 완료 (2026-05-21) | ✅ done |
| U-4 | 이미 제출한 수검자 재접속 시 안내 | 🟠 | 없음 | 동일 인적사항 재접속 시 "이미 제출됨" 또는 결과 링크 안내 |
| U-5 | 검사 도중 세션 만료 처리 | 🟡 | 임시저장은 있으나 명시적 안내 없음 | 오래 방치 후 재접속 시 이어하기 안내 화면 |
| U-6 | 접속 불가 링크 안내 | 🟡 | 단순 에러 메시지 | 만료·비활성 링크 접속 시 사유와 담당자 연락처 안내 |

---

## 착수 순서 권장

```
1단계 (운영 시작 전 필수)
  I-1  프로세스 관리 (systemd)
  I-2  HTTPS 적용
  S-1  bcrypt 비밀번호 해싱

2단계 (첫 실사용 후 즉시)
  R-1  제출 목록 화면
  R-3  수검자 결과 재접근
  L-1  링크 만료일

3단계 (운영 안정화)
  L-3  링크별 실시 현황
  R-2  결과 일괄 내보내기
  I-3  에러 모니터링
  I-4  DB 백업 확인

4단계 (규모 확장 시)
  S-2  세션 DB 이전
  S-3  Rate limiting
  P-1  채점 비동기 분리
  I-5  배포 자동화
```
