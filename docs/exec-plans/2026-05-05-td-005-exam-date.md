# Execution Plan

## Task Title
- TD-005: 검사 실시일 입력 UI 추가 및 나이 계산 기준일 반영

## Request Summary
- 인적사항 입력 화면에 검사 실시일 필드 추가 (생년월일 위에 위치)
- 기본값: 오늘 날짜, 검사 실시자가 변경 가능
- 백엔드에서 age 계산 시 `date.today()` 대신 profile의 `exam_date` 사용
- 채점 시에도 저장된 `exam_date` 기준으로 norm 선택 → TD-005 해소

## Goal
- `profile.exam_date`가 age_range 비교 기준일이 됨
- 실시 시점과 채점 시점이 달라도 동일한 variant 선택 보장
- 생일 경계 케이스에서 잘못된 norm 채점 방지

## Preflight Checklist
- [x] AGENTS.md 확인
- [x] exec-plan 먼저 작성
- [x] 관련 코드 탐색 완료
- [x] 수정 전 스크린샷 (시작화면 캡처 완료: /tmp/profile_before.png)
- [x] 검증 방법 정의

## 변경 대상

### 1. `frontend/src/pages/assessment/types.ts`
`Profile`에 `exam_date` 필드 추가:
```ts
exam_date?: string   // YYYY-MM-DD, 검사 실시일
```

### 2. `frontend/src/pages/assessment/steps/ProfileStep.tsx`
- `examDate` state 추가 (기본값: 오늘 날짜 `new Date().toISOString().slice(0,10)`)
- `validate()`: examDate 비어있으면 "검사 실시일" 반환
- `buildProfile()`: 항상 `exam_date: examDate` 포함
- UI: 생년월일 바로 위에 날짜 입력 필드 추가

### 3. `app/services/admin/assessment_links.py`
`_profile_matches_sub_test`에서 `date.today()` → `profile.exam_date` 사용:
```python
as_of = _safe_parse_date(profile.get("exam_date")) or date.today()
age_now = _age_detail(birth_day, as_of)
```

## 검증 방법
- UI: 인적사항 화면에 검사 실시일 필드가 생년월일 위에 표시되는지 확인
- UI: 기본값이 오늘 날짜인지 확인
- UI: 날짜 변경 시 profile에 반영되는지 확인
- 백엔드: exam_date 없는 구 profile도 date.today() fallback으로 동작 확인

## Screenshot
- Before: /tmp/profile_before.png
- After: /tmp/profile_after.png

## Related Documents
- [docs/technical-debt.md](/Users/mac/insight_/2.0-modular/docs/technical-debt.md)
- [AGENTS.md](/Users/mac/insight_/2.0-modular/AGENTS.md)
